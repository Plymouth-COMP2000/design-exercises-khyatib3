<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/guest_starters_page"
    android:padding="8dp"
    android:orientation="vertical"
    tools:context=".Guest_Starters_Activity">

    <!-- home button and sidebar button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="8dp">

        <!-- home button -->
        <ImageButton
            android:id="@+id/g_startersToHomeBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/backHomeTxt"
            android:src="@drawable/ic_arrow_back"/>

        <TextView
            android:id="@+id/startersTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/starterLbl"
            android:fontFamily="@font/abril_fatface"
            android:textSize="26sp"
            android:textStyle="bold"
            android:gravity="center" />

    </LinearLayout>

    <!-- search bar -->
    <SearchView
        android:id="@+id/startersSearchBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:queryHint="Search starters by keyword..."
        android:background="@drawable/rounded_input" />

    <!-- gap -->
    <LinearLayout
        android:layout_width="30dp"
        android:layout_height="30dp">
    </LinearLayout>

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
    <!-- menu items data -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/g_startersRecyclerView"
        android:layout_width="394dp"
        android:layout_height="604dp"
        android:layout_weight="1"
        android:scrollbars="vertical" />
    </ScrollView>

</LinearLayout>


